---
- name: distribute ssh private keys
  hosts: all
  tasks:
    - name: Copy key
      ansible.builtin.copy:
        src: "{{ key_path }}"
        dest: "/home/{{ ansible_user }}/.ssh/id_rsa"
        owner: "{{ ansible_user }}"
        group: "{{ ansible_user }}"
        mode: '0700'
